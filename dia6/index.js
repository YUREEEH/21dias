let multiplicante = Number(prompt("Digite o numero que precisa da taboada"))

for (let multiplicador = 0; multiplicador <= 10; multiplicador++){
    console.log(multiplicante + " x " + multiplicador + " = " + (multiplicante*multiplicador))
}